[alias]
lcov = [
  "llvm-cov",
  "--locked",
  "--all-features",
  "--workspace",
  "--no-report",
  "nextest",
]
lcov-json = [
  "llvm-cov",
  "report",
  "--codecov",
  "--output-path",
  "cov.json",
  "--ignore-filename-regex",
  "cli/src/main",
]
lcov-assert = [
  "llvm-cov",
  "report",
  "--fail-under-functions",
  "80",
  "--fail-under-lines",
  "80",
  "--fail-under-file-lines",
  "80",
  "--fail-under-regions",
  "80",
  "--ignore-filename-regex",
  # Ignore the main.rs file in the CLI crate, as it is not covered by tests and will always fail coverage checks.
  # variant 実装でmodelもカバーされるようになるはずなので、そのタイミングでignoreから外す
  "cli/src/main|model",
]
ins-self = ["install", "--path", "crates/reportage-cli", "--locked"]
